# JavaScript Utils - Parsing
## Purpose
Provides a way to parse various file types.

## parse-csv.js
Introduces `JSParser.fromCSV(...): (object)` and `JSParser.toCSV(...): (string)` functions to parse and create comma separated value (CSV) files.
